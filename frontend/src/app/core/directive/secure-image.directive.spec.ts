import { SecureImageDirective } from './secure-image.directive';

describe('SecureImageDirective', () => {
  it('should create an instance', () => {
    const directive = new SecureImageDirective();
    expect(directive).toBeTruthy();
  });
});
